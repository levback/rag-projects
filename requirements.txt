# ── LLM providers ────────────────────────────────────────────────────────────
openai>=1.30.0
anthropic>=0.28.0
boto3>=1.34.0                 # Amazon Bedrock

# ── Embeddings & vector stores ────────────────────────────────────────────────
chromadb>=0.5.0
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# ── Tokenization ─────────────────────────────────────────────────────────────
tiktoken>=0.7.0

# ── Config & utilities ────────────────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.7.0

# ── Document analysis ─────────────────────────────────────────────────────────
pdfplumber>=0.11.0          # PDF text extraction
nltk>=3.8.0                 # Sentence tokenisation
transformers>=4.41.0,<5.0.0  # HuggingFace summarisation / QG / QA pipelines (v5 removed seq2seq tasks)
torch>=2.3.0                # PyTorch backend for transformers
accelerate>=0.30.0          # HF accelerated inference

# ── Knowledge graphs (GraphRAG) ───────────────────────────────────────────────
networkx>=3.2

# ── Web retrieval (RealtimeRAG, ResearchAgent) ────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
duckduckgo-search>=6.0      # pip name: ddgs

# ── LangChain ecosystem (MultiDocRAG, AgenticRAG, LangChainRAGAgent) ─────────
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langchain-huggingface>=0.1.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-aws>=0.2.0        # Bedrock LangChain integration

# ── Multimodal RAG (optional — heavy dependencies) ────────────────────────────
# Uncomment to enable Docling-based PDF parsing + image captioning:
# docling>=2.0
# Pillow>=10.0

# ── Local model inference (optional, already pulled by transformers above) ────
# transformers>=4.41.0
# torch>=2.3.0
# accelerate>=0.30.0

# ── Logging ───────────────────────────────────────────────────────────────────
python-json-logger>=2.0.7

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
